# GitHub Actions 环境变量配置示例
# 复制此文件内容到 GitHub 仓库的 Settings > Secrets and variables > Actions

# 必需的 Secrets
NPM_TOKEN=npm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx  # npm 访问令牌

# 可选的 Secrets
GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}  # GitHub 自动提供，用于创建标签

# 可选的环境变量 (在 workflow 中可以自定义)
NODE_VERSION=18                           # Node.js 版本
REGISTRY_URL=https://registry.npmjs.org   # npm 仓库地址
PACKAGE_ACCESS=public                     # 包访问权限

# 获取 NPM_TOKEN 的步骤：
# 1. 登录 npm: npm login
# 2. 创建令牌: npm token create --access public
# 3. 复制生成的令牌到 GitHub Secrets